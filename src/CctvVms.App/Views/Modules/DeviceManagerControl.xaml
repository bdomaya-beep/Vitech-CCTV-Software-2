<UserControl x:Class="CctvVms.App.Views.Modules.DeviceManagerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Refresh" Width="80" Margin="0,0,8,0" Command="{Binding RefreshCommand}" />
            <Button Content="Remove" Width="80" Command="{Binding RemoveCommand}" />
            <Button Content="Test Connection" Width="120" Margin="8,0,0,0" Command="{Binding TestConnectionCommand}" />
            <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" Foreground="#9BB1CC" Text="{Binding ConnectionTestResult}" />
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Devices}" SelectedItem="{Binding SelectedDevice}" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="IP Address" Binding="{Binding IpAddress}" Width="180" />
                <DataGridTextColumn Header="NVR Type" Binding="{Binding NvrType}" Width="180" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
